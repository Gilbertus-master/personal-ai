Discovery report written to `scripts/frontend_app/parts/part4/discovery.md`.

**Key findings:**

- **34 endpoints confirmed** via OpenAPI — full coverage for the module spec, plus extras (scan, communication-plan, execute-communication)
- **No backend RBAC** on `/compliance/*` endpoints (Gilbertus API is unauthed) — enforce client-side via `<RbacGate>` + `@gilbertus/rbac`
- **Existing patterns** are clear: Zustand stores for UI state, TanStack React Query for data, `customFetch<T>()` for API, CSS variables for theming, `cn()` for class merging
- **No gaps** for core features. Minor limitations: no pagination metadata, no PATCH/DELETE (workflow-driven by design), no fulltext search
- **Complex features:** Matter phase stepper (11 phases), 5x5 risk heatmap, editable RACI matrix
- **AI operations** (research, doc generation, reports) take 10-20s — need prominent loading states
- **~25-30 components, ~15 pages** estimated
[ARCHITECT] Started: 2026-03-29T13:29:10.814649+00:00
======================================================================

