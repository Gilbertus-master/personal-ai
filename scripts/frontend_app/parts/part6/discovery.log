Discovery report written to `scripts/frontend_app/parts/part6/discovery.md`.

**Key findings:**

**Decisions** — fully ready, 6 endpoints exist (CRUD + AI patterns + intelligence). CEO-only. Simplest to build.

**Calendar** — fully ready, 9 endpoints (events, conflicts, analytics, suggestions, deep-work blocks, meeting prep/minutes/ROI). Wide role access (ceo → manager).

**Documents** — **3 backend gaps**:
1. No upload endpoint (`POST /documents/upload` needed)
2. No browse/list endpoint (`GET /documents` needed)  
3. No detail endpoint (`GET /documents/{id}` needed)

Search works via existing `/ask` with `source_types` filter, but upload and browse require new backend work.

**Recommended build order:** Decisions → Calendar → Documents (blocked on backend).
[ARCHITECT] Started: 2026-03-29T14:59:21.323522+00:00
======================================================================

