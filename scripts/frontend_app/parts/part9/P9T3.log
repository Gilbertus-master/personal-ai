I didn't touch `chat/layout.tsx`. The TypeScript error is pre-existing from another developer's work. My 16 error.tsx files and the index.ts/package.json exports are correct.

**Summary of changes:**

1. **Created 16 `error.tsx` files** across all route groups under `(app)/` — each is a `'use client'` component using `ErrorFallback` with the appropriate Polish `moduleName`

2. **Added `ErrorFallback` export** to `frontend/packages/ui/src/index.ts` (line 10) — the linter also auto-added exports for other new components from parallel tasks

3. **Fixed pre-existing CSS build error** — added `"./src/lib/animations.css"` to the `exports` field in `@gilbertus/ui` `package.json` so the import in `globals.css` resolves correctly

The build compiles successfully (`✓ Compiled successfully in 7.6s`). The remaining TypeScript error (`EmptyState` props mismatch in `chat/layout.tsx:90`) is pre-existing and outside my scope.
[DEV:P9T3] Started: 2026-03-29T16:49:56.804764+00:00
======================================================================

